# EDHREC Commander Randomizer Requirements

# Image processing for card images
Pillow>=10.0.0

# HTTP requests for Scryfall API
requests>=2.31.0

# Note: scrape_edhrec_api.py uses only Python standard library (urllib, json, csv)
# No additional dependencies needed for the API-based scraper!
