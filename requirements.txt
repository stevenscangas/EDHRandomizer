# EDHREC Commander Randomizer Requirements

# Image processing for card images
Pillow>=10.0.0

# HTTP requests for Scryfall API
requests>=2.31.0

# Browser automation for EDHRec scraping
playwright>=1.40.0

# CSV handling (built-in, no need to install)
# json handling (built-in, no need to install)
